server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devs4j/microservices-config
          searchPaths: menu
          username: devs4j
          password: h0l41234!